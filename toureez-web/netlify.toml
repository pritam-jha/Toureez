[build]
  command = "npm run build"
  publish = "dist"

# Single-page app: let React Router handle all routes client-side instead of
# Netlify 404ing on direct loads of e.g. /app/search or /admin/vendors.
[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200
